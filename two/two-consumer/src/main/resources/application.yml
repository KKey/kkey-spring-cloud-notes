server:
  port: 7777

spring:
  application:
    name: two-consumer

eureka:
  client:
    service-url:
      defaultZone: http://localhost:5555/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always